---
import CatalogCityHub from '../../components/CatalogCityHub.astro';
import { cities, cityBySlug } from '../../data/cities';

export async function getStaticPaths() {
	return cities.map((c) => ({ params: { city: c.slug } }));
}

const { city: citySlug } = Astro.params;
const city = cityBySlug[citySlug!];
---

<CatalogCityHub
	hub="provoloka"
	hubLabel="Проволока нержавеющая"
	hubLabelGenitive="проволоку нержавеющую"
	hubHref="/provoloka-nerzhaveyushchaya/"
	measure="диаметр"
	city={city}
/>
